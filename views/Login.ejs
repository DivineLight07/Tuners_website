<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("partials/head", { title: "TUNERS — Login" }) %>
</head>
<body>

  <%- include("partials/navbar") %>

  <div class="flex-1 flex items-center justify-center p-4">
    <div class="w-full max-w-md" data-aos="fade-up">
      <!-- Auth Layout Structure -->
      <div class="bg-card backdrop-blur-md border border-border rounded-[20px] p-8 sm:p-10 shadow-2xl">
        <div class="flex justify-center mb-6">
          <div class="w-12 h-12 rounded-xl bg-primary/20 flex items-center justify-center">
            <i data-lucide="log-in" class="w-6 h-6 text-primary"></i>
          </div>
        </div>
        
        <div class="text-center mb-8">
          <h2 class="text-3xl font-display tracking-wide mb-2">Welcome back</h2>
          <p class="text-sm text-muted-foreground">Log in to your account</p>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
          <div class="mb-4 p-3 rounded-lg bg-destructive/10 text-destructive text-sm text-center">
            <%- error %>
          </div>
        <% } %>

        <a href="/auth/google" class="w-full h-12 mb-6 flex items-center justify-center gap-2 bg-white hover:bg-gray-100 text-gray-900 font-sans font-medium rounded-xl transition-all shadow border border-gray-200">
          <svg width="18" height="18" viewBox="0 0 48 48">
            <path fill="#EA4335" d="M24 9.5c3.1 0 5.8 1.1 8 2.9l6-6C34.5 3.1 29.6 1 24 1 14.8 1 7 6.7 3.7 14.7l7 5.4C12.5 13.6 17.8 9.5 24 9.5z"/>
            <path fill="#4285F4" d="M46.5 24.5c0-1.6-.1-3.1-.4-4.5H24v8.5h12.7c-.6 3-2.3 5.5-4.8 7.2l7.4 5.7c4.3-4 6.8-9.9 6.8-16.9z"/>
            <path fill="#FBBC05" d="M10.7 28.6A14.6 14.6 0 0 1 9.5 24c0-1.6.3-3.2.8-4.6l-7-5.4A23.9 23.9 0 0 0 .5 24c0 3.9.9 7.5 2.6 10.8l7.6-6.2z"/>
            <path fill="#34A853" d="M24 46.5c5.5 0 10.2-1.8 13.6-4.9l-7.4-5.7c-1.8 1.2-4.1 1.9-6.2 1.9-6.2 0-11.4-4.2-13.3-9.8l-7.6 6.2C7 42.7 14.9 46.5 24 46.5z"/>
          </svg>
          Continue with Google
        </a>

        <div class="relative mb-6">
          <div class="absolute inset-0 flex items-center">
            <div class="w-full border-t border-border"></div>
          </div>
          <div class="relative flex justify-center text-xs uppercase">
            <span class="bg-card px-3 text-muted-foreground">or</span>
          </div>
        </div>

        <div id="login-wrong-err" class="hidden mb-4 p-3 rounded-lg bg-destructive/10 text-destructive text-sm text-center">Incorrect email or password.</div>

        <form id="login-form" class="space-y-4" novalidate>
          <div class="space-y-2">
            <label for="login-email" class="text-xs font-semibold tracking-widest uppercase text-muted-foreground">Email</label>
            <div class="relative">
              <i data-lucide="mail" class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground"></i>
              <input
                id="login-email"
                type="email"
                required
                placeholder="you@miuegypt.edu.eg"
                class="w-full h-12 pl-10 pr-4 bg-input border border-border rounded-lg text-sm text-white placeholder-white/30 focus:outline-none focus:border-primary focus:ring-2 focus:ring-primary/30 transition-all"
              />
            </div>
            <span class="hidden text-[0.72rem] text-destructive mt-1" id="login-email-err">Enter a valid email address.</span>
          </div>
          <div class="space-y-2">
            <div class="flex items-center justify-between">
              <label for="login-pass" class="text-xs font-semibold tracking-widest uppercase text-muted-foreground">Password</label>
            </div>
            <div class="relative">
              <i data-lucide="lock" class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground"></i>
              <input
                id="login-pass"
                type="password"
                required
                placeholder="••••••••"
                class="w-full h-12 pl-10 pr-10 bg-input border border-border rounded-lg text-sm text-white placeholder-white/30 focus:outline-none focus:border-primary focus:ring-2 focus:ring-primary/30 transition-all"
              />
              <button type="button" onclick="togglePasswordVisibility('login-pass', this)" class="absolute right-3 top-1/2 -translate-y-1/2 text-white/50 hover:text-white transition-colors z-10">
                <i data-lucide="eye" class="w-4 h-4 icon-eye block"></i>
                <i data-lucide="eye-off" class="w-4 h-4 icon-eye-off hidden"></i>
              </button>
            </div>
            <span class="hidden text-[0.72rem] text-destructive mt-1" id="login-pass-err">Password is required.</span>
          </div>
          
          <button type="submit" class="w-full h-12 bg-primary hover:bg-[#1a48a8] text-white font-display text-lg tracking-widest rounded-xl transition-all shadow-lg hover:shadow-xl hover:-translate-y-[1px] flex items-center justify-center">
            Login
          </button>
        </form>

        <div class="mt-8 text-center text-sm">
          <span class="text-muted-foreground">Not a member yet? </span>
          <a href="/apply" class="font-medium text-primary hover:text-white transition-colors">Apply to join</a>
        </div>
      </div>
    </div>
  </div>

  <div id="Msg"></div>

  <%- include("partials/footer") %>

  <%- include("partials/scripts") %>
  <script src="/js/login.js"></script>
</body>
</html>
